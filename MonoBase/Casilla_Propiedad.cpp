//
// Created by alonso on 31/05/19.
//

#include <iostream>
#include "Casilla_Propiedad.h"


void Casilla_Propiedad::accion(Jugador huesped) {
    int n;
    if (!ocupada){
        std::cout << "#OPCIONES#"<< std::endl << "1. Comprar." << std::endl << "2. Dejar a subasta" << std::endl << "3. Hipotecar" << "Escoja la acción a tomar : ";
        std::cin >> n;
        switch(n){
            case :
        }
    }
}